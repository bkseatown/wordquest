/* =====================================================
   Word Quest v2 — style.css  (clean rebuild)
   
   Design principles:
   · Default is genuinely LIGHT — cream tiles, warm bg
   · Keys: one treatment only — raised shadow, clean
   · Vowels: single soft teal accent-line, nothing else  
   · No competing outlines/glows on keys
   · 9px key gap for breathing room
   · State scaling: absent 0.88, correct 0.94, present 1.0
   · Dark themes feel "rich", not oppressive
   ===================================================== */

/* ── Feedback colors: NEVER change per theme ────────── */


/* ══════════════════════════════════════════════════════
   THEME RECIPES
   Each theme provides ONLY brand tokens.
   Tile face stays light/cream in ALL themes.
   Background and keyboard adopt the brand palette.
   ══════════════════════════════════════════════════════ */

/* ── DEFAULT: warm, classroom-friendly light theme ──── */
:root,


/* ── DARK: slate/charcoal, blue accent ─────────────── */


/* ── SEAHAWKS: navy #002244 / action green #69BE28 / wolf gray #A5ACAF ── */


/* ── HUSKIES: purple #4B2E83 / gold #B7A57A / metallic gold #85754D ── */


/* ── COFFEE / STARBUCKS: green #00704A / espresso #362415 / cream #EAC784 */


/* ── MATRIX ─────────────────────────────────────────── */


/* ── SUNSET ─────────────────────────────────────────── */


/* ── SUPERMAN: blue #003087 / red #c62828 ────────────── */


/* ── IRON MAN: red #AA0505 / bright gold #FBCA03 / maroon #4B0908 ──── */


/* ── MARVEL: red #E23636 / black / blue #518CCA ──────── */


/* ── DEMON HUNTER / K-pop synthwave ─────────────────── */


/* ── HARLEY QUINN: hot pink #ED3A96 / red #BE120A / ice blue #35A2BF ── */


/* ── KUROMI: deep purple #514283 / hot pink #F181B2 / near-black ──────── */


/* ── OCEAN ───────────────────────────────────────────── */


/* ══════════════════════════════════════════════════════
   RESET + BASE
   ══════════════════════════════════════════════════════ */
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
html { height: 100%; overflow: hidden; }

body {
  font-family:    var(--font);
  background:     linear-gradient(145deg, var(--page-bg) 0%, var(--page-bg2) 100%);
  color:          var(--text);
  height:         100dvh;
  overflow:       hidden;
  display:        flex;
  flex-direction: column;
  align-items:    center;
  transition:     background 0.3s, color 0.3s;
}

/* ── Loading ─────────────────────────────────────────── */
#loading-screen {
  position:        fixed;
  inset:           0;
  background:      var(--page-bg);
  display:         flex;
  flex-direction:  column;
  align-items:     center;
  justify-content: center;
  gap:             14px;
  z-index:         100;
  color:           var(--text-muted);
}
.spinner {
  width:         34px;
  height:        34px;
  border:        4px solid rgba(128,128,128,0.2);
  border-top-color: var(--brand);
  border-radius: 50%;
  animation:     spin 0.8s linear infinite;
}
.hidden { display: none !important; }

/* ── Header ──────────────────────────────────────────── */
header {
  width:           100%;
  max-width:       560px;
  height:          var(--header-h);
  flex-shrink:     0;
  padding:         0 16px;
  display:         flex;
  align-items:     center;
  justify-content: space-between;
  background:      var(--header-bg);
  border-bottom:   1px solid var(--header-border);
  position:        relative;
  z-index:         20;
}

.logo {
  font-family: var(--font-logo);
  font-size:   1.3rem;
  font-weight: 900;
  color:       var(--text);
}
.logo span { color: var(--brand); }

.header-right { display: flex; align-items: center; gap: 10px; }

.icon-btn {
  width:         34px;
  height:        34px;
  border:        1.5px solid var(--header-border);
  border-radius: 8px;
  background:    transparent;
  color:         var(--text-muted);
  cursor:        pointer;
  display:       flex;
  align-items:   center;
  justify-content: center;
  transition:    background 0.15s;
}
.icon-btn:hover { background: rgba(128,128,128,0.1); color: var(--text); }

#new-game-btn {
  background:    var(--brand);
  color:         var(--brand-text);
  border:        none;
  border-radius: 8px;
  padding:       7px 16px;
  font-family:   var(--font);
  font-size:     0.85rem;
  font-weight:   700;
  cursor:        pointer;
  transition:    background 0.15s;
  white-space:   nowrap;
}
#new-game-btn:hover { background: var(--brand-dk); }
#new-game-btn.pulse { animation: pulse 1.4s ease infinite; }

/* ── Focus bar ───────────────────────────────────────── */
.focus-bar {
  width:         100%;
  max-width:     560px;
  height:        var(--focus-h);
  flex-shrink:   0;
  display:       flex;
  align-items:   center;
  gap:           8px;
  padding:       0 16px;
  background:    var(--focusbar-bg);
  border-bottom: 1px solid var(--header-border);
  position:      relative;
  z-index:       15;
}

.focus-label {
  font-size:     0.68rem;
  font-weight:   700;
  text-transform: uppercase;
  letter-spacing: 0.8px;
  color:         var(--text-muted);
  white-space:   nowrap;
  flex-shrink:   0;
}

.focus-bar select {
  flex:          1;
  min-width:     0;
  padding:       5px 8px;
  border:        1.5px solid var(--header-border);
  border-radius: 7px;
  font-family:   var(--font);
  font-size:     0.88rem;
  font-weight:   700;
  background:    var(--panel-bg);
  color:         var(--text);
  cursor:        pointer;
}

.focus-hint {
  font-size:     0.7rem;
  font-weight:   700;
  color:         var(--brand);
  background:    transparent;
  border:        1px solid var(--brand);
  border-radius: 99px;
  padding:       2px 9px;
  white-space:   nowrap;
  flex-shrink:   0;
}

/* ── Settings overlay ───────────────────────────────── */
.settings-panel {
  position:      absolute;
  top:           calc(var(--header-h) + var(--focus-h));
  left:          50%;
  transform:     translateX(-50%);
  width:         min(560px, 100vw);
  background:    var(--panel-bg);
  border:        1px solid var(--panel-border);
  border-top:    none;
  border-radius: 0 0 var(--radius-panel) var(--radius-panel);
  box-shadow:    var(--panel-shadow);
  z-index:       40;
  animation:     slideDown 0.18s ease;
}

.settings-header {
  display:         flex;
  align-items:     center;
  justify-content: space-between;
  padding:         11px 16px;
  border-bottom:   1px solid var(--panel-border);
  font-weight:     700;
  font-size:       0.88rem;
  color:           var(--text);
}
.close-btn {
  background: transparent; border: none;
  color: var(--text-muted); cursor: pointer;
  font-size: 1rem; padding: 2px 7px; border-radius: 4px;
}
.close-btn:hover { background: rgba(128,128,128,0.1); }

.settings-body {
  display:               grid;
  grid-template-columns: 1fr 1fr;
  background:            var(--panel-border);
  gap:                   1px;
}
.setting-row {
  display:        flex;
  flex-direction: column;
  gap:            4px;
  padding:        9px 14px;
  background:     var(--panel-bg);
}
.setting-row-full { grid-column: 1 / -1; }
.setting-row label {
  font-size:     0.68rem;
  font-weight:   700;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  color:         var(--text-muted);
}
.setting-row select {
  padding:    5px 7px;
  border:     1.5px solid var(--panel-border);
  border-radius: 6px;
  font-family: var(--font);
  font-size:  0.82rem;
  background: var(--panel-bg);
  color:      var(--text);
  cursor:     pointer;
}

/* ── Main ────────────────────────────────────────────── */
main {
  display:         flex;
  flex-direction:  column;
  align-items:     center;
  justify-content: center;
  gap:             10px;
  flex:            1;
  width:           100%;
  max-width:       560px;
  padding:         10px 12px;
  overflow:        hidden;
}

/* ── Board zone ──────────────────────────────────────── */
.board-zone {
  display:        flex;
  flex-direction: column;
  align-items:    center;
  gap:            10px;
  flex-shrink:    0;
}

.board-plate {
  background:    var(--plate-bg);
  border-radius: var(--radius-plate);
  padding:       14px;
  border:        1px solid var(--plate-border);
  box-shadow:    0 4px 24px rgba(0,0,0,0.12);
}

/* ── Game board ──────────────────────────────────────── */
#game-board {
  display:               grid;
  grid-template-columns: repeat(var(--word-length, 5), var(--tile-size));
  grid-template-rows:    repeat(var(--max-guesses, 6), var(--tile-size));
  gap:                   var(--gap-tile);
}

/* ── Tiles ───────────────────────────────────────────── */
.tile {
  width:           var(--tile-size);
  height:          var(--tile-size);
  border:          2px solid var(--tile-border);
  border-radius:   var(--radius-tile);
  display:         flex;
  align-items:     center;
  justify-content: center;
  font-size:       calc(var(--tile-size) * 0.44);
  font-weight:     700;
  background:      var(--tile-face);
  color:           var(--tile-text);
  box-shadow:      var(--tile-shadow);
  user-select:     none;
  position:        relative;
  overflow:        hidden;
  transition:      border-color 0.1s, transform 0.18s;
}

/* Subtle gloss */
.tile::after {
  content:       '';
  position:      absolute;
  top: 0; left: 0; right: 0;
  height:        44%;
  background:    linear-gradient(180deg, rgba(255,255,255,0.3) 0%, transparent 100%);
  border-radius: var(--radius-tile) var(--radius-tile) 0 0;
  pointer-events: none;
}

.tile.filled { border-color: var(--tile-filled-border); }

[data-motion="on"] .tile.just-typed { animation: tile-jiggle 0.22s ease; }

/* Result states — scale applied here */
.tile.correct {
  background:   linear-gradient(160deg, var(--correct-lt), var(--correct));
  color:        #fff;
  border-color: var(--correct-dk);
  box-shadow:   0 3px 0 var(--correct-dk), 0 1px 8px rgba(22,163,74,0.25);
  transform:    scale(0.94);
}
.tile.present {
  background:   linear-gradient(160deg, var(--present-lt), var(--present));
  color:        #fff;
  border-color: var(--present-dk);
  box-shadow:   0 3px 0 var(--present-dk);
  transform:    scale(1.0);
}
.tile.absent {
  background:   linear-gradient(160deg, #c8d5e0, var(--absent));
  color:        rgba(255,255,255,0.85);
  border-color: var(--absent-dk);
  box-shadow:   0 2px 0 var(--absent-dk);
  transform:    scale(0.88);
}

.tile.flip  { animation: tile-flip 0.45s ease forwards; }
.tile.shake { animation: shake 0.4s ease; }

/* ── Gameplay audio (below board, above keyboard) ─────── */
.gameplay-audio {
  display:  flex;
  gap:      10px;
}

.audio-ghost-btn {
  display:     flex;
  align-items: center;
  gap:         5px;
  padding:     6px 15px;
  background:  var(--panel-bg);
  border:      1.5px solid var(--panel-border);
  border-radius: 99px;
  color:       var(--text-muted);
  font-family: var(--font);
  font-size:   0.8rem;
  font-weight: 600;
  cursor:      pointer;
  transition:  background 0.15s, color 0.15s, border-color 0.15s;
}
.audio-ghost-btn:hover {
  background:   var(--key-hover);
  border-color: var(--brand);
  color:        var(--text);
}
.audio-ghost-btn svg { flex-shrink: 0; }

/* ── Keyboard ────────────────────────────────────────── */
#keyboard {
  display:        flex;
  flex-direction: column;
  align-items:    center;
  gap:            var(--gap-key);
  width:          min(520px, var(--playfield-width));
  flex-shrink:    0;
}

.key-row {
  display:         flex;
  gap:             var(--gap-key);
  justify-content: center;
  width:           100%;
}

.key {
  height:        var(--key-h);
  min-width:     var(--key-min-w);
  flex:          1;
  max-width:     52px;
  padding:       0;
  border:        none;
  border-radius: var(--radius-key);
  background:    var(--key-bg);
  box-shadow:    var(--key-shadow);
  color:         var(--key-text);
  font-family:   var(--font);
  font-size:     0.9rem;
  font-weight:   700;
  cursor:        pointer;
  touch-action:  manipulation;
  user-select:   none;
  position:      relative;
  overflow:      hidden;
  transition:    background 0.18s, color 0.18s, transform 0.18s, box-shadow 0.18s, opacity 0.18s;
}

.key:hover  { background: var(--key-hover); }

/* ── Vowel cue ───────────────────────────────────── */

/* ── Vowel cue (Phonics Hint only): subtle inner ring ──── */
html[data-hint="on"] .key.vowel:not(.correct):not(.present):not(.absent){
  box-shadow:
    var(--key-shadow),
    inset 0 0 0 2px color-mix(in oklab, var(--vowel-accent) 70%, transparent);
}
/* No ring, no glow — just a clean accent underline */
/* Wide keys */
.key.wide { flex: 1.6; max-width: 80px; font-size: 0.78rem; }

/* Bounce — only when motion is on */
[data-motion="on"] .key.bounce { animation: key-bounce 0.14s ease; }

/* ── Key result states ───────────────────────────────── */
.key.correct {
  background:  linear-gradient(160deg, var(--correct-lt), var(--correct));
  color:       #fff;
  box-shadow:  0 2px 0 var(--correct-dk);
  transform:   scale(0.94);
}
.key.present {
  background:  linear-gradient(160deg, var(--present-lt), var(--present));
  color:       #fff;
  box-shadow:  0 2px 0 var(--present-dk);
  transform:   scale(1.0);
}
.key.absent {
  background:  var(--absent);
  color:       rgba(255,255,255,0.6);
  box-shadow:  none;
  transform:   scale(0.88);
  opacity:     0.65;
}

/* In-play inflation — just size, no extra border/ring */
.key.in-play:not(.correct):not(.absent):not(.present) {
  transform: scale(1.08);
}

/* Duplicate pulse — uses accent color, never green */
.key.dupe-pulse {
  animation: dupe-pulse 2.2s ease infinite;
}

/* Remove vowel underline once state is applied */
.key.correct,
.key.present,
.key.absent {
  border-bottom: none;
}

/* ── Projector mode ──────────────────────────────────── */
[data-projector="on"] .tile {
  border-width: 3px;
  font-size:    calc(var(--tile-size) * 0.50);
  font-weight:  800;
}
[data-projector="on"] .key {
  font-size: 1.05rem;
  font-weight: 800;
}
[data-projector="on"] {
  --tile-text: #000;
}

/* ── Reduced motion ──────────────────────────────────── */
[data-motion="off"] * {
  animation-duration:        0.001ms !important;
  animation-iteration-count: 1 !important;
  transition-duration:       0.001ms !important;
}


/* ── Animations ──────────────────────────────────────── */
@keyframes tile-jiggle {
  0%   { transform: scale(1); }
  30%  { transform: scale(1.1) rotate(-1.5deg); }
  60%  { transform: scale(1.07) rotate(1deg); }
  85%  { transform: scale(1.02); }
  100% { transform: scale(1); }
}

@keyframes tile-flip {
  0%   { transform: rotateX(0deg);   }
  42%  { transform: rotateX(-90deg); }
  58%  { transform: rotateX(-90deg); }
  100% { transform: rotateX(0deg);   }
}

@keyframes shake {
  0%, 100% { transform: translateX(0);    }
  15%       { transform: translateX(-6px); }
  35%       { transform: translateX(6px);  }
  55%       { transform: translateX(-4px); }
  75%       { transform: translateX(4px);  }
}

@keyframes key-bounce {
  0%   { transform: scale(1) translateY(0);    box-shadow: var(--key-shadow); }
  40%  { transform: scale(0.93) translateY(3px); box-shadow: none; }
  100% { transform: scale(1) translateY(0);    box-shadow: var(--key-shadow); }
}

@keyframes dupe-pulse {
  0%, 100% { box-shadow: var(--key-shadow); }
  50%       { box-shadow: var(--key-shadow), 0 0 10px 3px var(--accent); }
}

@keyframes pulse {
  0%, 100% { transform: scale(1); }
  50%       { transform: scale(1.06); }
}

@keyframes spin    { to { transform: rotate(360deg); } }

@keyframes slideDown {
  from { transform: translateX(-50%) translateY(-6px); opacity: 0; }
  to   { transform: translateX(-50%) translateY(0);    opacity: 1; }
}

@keyframes pop-in {
  from { transform: scale(0.3) rotate(-10deg); opacity: 0; }
  to   { transform: scale(1)   rotate(0);      opacity: 1; }
}

/* ── Toast ───────────────────────────────────────────── */
#toast {
  position:       fixed;
  top:            calc(var(--header-h) + var(--focus-h) + 12px);
  left:           50%;
  transform:      translateX(-50%) translateY(-6px);
  background:     rgba(15,20,30,0.92);
  color:          #f0f0f8;
  padding:        9px 20px;
  border-radius:  24px;
  font-size:      0.87rem;
  font-weight:    600;
  pointer-events: none;
  opacity:        0;
  transition:     opacity 0.2s, transform 0.2s;
  z-index:        200;
  max-width:      88vw;
  text-align:     center;
  border:         1px solid rgba(255,255,255,0.1);
}
#toast.visible { opacity: 1; transform: translateX(-50%) translateY(0); }

/* Duplicate toast */
#dupe-toast {
  position:       fixed;
  top:            calc(var(--header-h) + var(--focus-h) + 12px);
  left:           50%;
  transform:      translateX(-50%);
  background:     var(--panel-bg);
  color:          var(--text);
  padding:        12px 18px;
  border-radius:  14px;
  font-size:      0.86rem;
  font-weight:    600;
  z-index:        201;
  max-width:      88vw;
  text-align:     center;
  border:         1px solid var(--accent);
  display:        flex;
  flex-direction: column;
  gap:            8px;
  align-items:    center;
  box-shadow:     0 4px 20px rgba(0,0,0,0.3);
}
#dupe-toast .dupe-dismiss-row {
  display:     flex;
  gap:         10px;
  align-items: center;
  font-size:   0.75rem;
  color:       var(--text-muted);
}
#dupe-toast button {
  background:    var(--brand);
  border:        none;
  border-radius: 99px;
  padding:       3px 12px;
  font-family:   var(--font);
  font-size:     0.75rem;
  font-weight:   700;
  cursor:        pointer;
  color:         var(--brand-text);
}

/* ── Confetti ─────────────────────────────────────────── */
#confetti-canvas {
  position:       fixed;
  inset:          0;
  pointer-events: none;
  z-index:        300;
  width:          100%;
  height:         100%;
}

/* ── Modal ───────────────────────────────────────────── */
#modal-overlay {
  position:        fixed;
  inset:           0;
  background:      rgba(0,0,0,0.55);
  backdrop-filter: blur(8px);
  z-index:         50;
  display:         flex;
  align-items:     center;
  justify-content: center;
  padding:         16px;
}

#end-modal {
  background:     var(--modal-bg);
  border-radius:  var(--radius-panel);
  border:         1px solid var(--panel-border);
  width:          100%;
  max-width:      400px;
  padding:        24px 20px;
  display:        flex;
  flex-direction: column;
  align-items:    center;
  gap:            9px;
  box-shadow:     0 24px 60px rgba(0,0,0,0.3);
  max-height:     90dvh;
  overflow-y:     auto;
}

#modal-result  { font-size: 1.4rem; font-weight: 700; color: var(--brand); }
#end-modal.loss #modal-result { color: var(--absent); }

#modal-word    { display: flex; gap: 4px; flex-wrap: wrap; justify-content: center; }
#modal-word span {
  width: 44px; height: 44px;
  display: flex; align-items: center; justify-content: center;
  border-radius: 10px;
  background:  var(--correct);
  color:       white;
  font-size:   1.35rem;
  font-weight: 700;
  box-shadow:  0 3px 0 var(--correct-dk);
  animation:   pop-in 0.3s ease calc(var(--i, 0) * 0.07s) both;
}
#end-modal.loss #modal-word span { background: var(--absent); box-shadow: 0 3px 0 var(--absent-dk); }

#modal-syllables { font-size: 0.9rem; font-weight: 600; color: var(--text-muted); letter-spacing: 1.5px; }
#modal-badge     { font-size: 0.73rem; background: transparent; border: 1px solid var(--panel-border); color: var(--brand); padding: 3px 12px; border-radius: 99px; font-weight: 700; }
#modal-guesses   { font-size: 0.83rem; font-weight: 600; color: var(--text-muted); }
.modal-divider   { width: 100%; height: 1px; background: var(--panel-border); margin: 3px 0; }
#modal-def       { font-size: 0.95rem; text-align: center; line-height: 1.55; max-width: 340px; }
#modal-sentence  { font-size: 0.87rem; text-align: center; color: var(--text-muted); font-style: italic; max-width: 340px; line-height: 1.5; }

#modal-fun-wrap  { background: var(--fun-bg); border: 1px solid var(--fun-border); border-radius: 10px; padding: 10px 14px; max-width: 340px; width: 100%; }
#modal-fun       { font-size: 0.84rem; text-align: center; color: var(--fun-text); line-height: 1.5; }

.audio-row { display: flex; gap: 6px; flex-wrap: wrap; justify-content: center; }
.audio-btn {
  padding:       6px 12px;
  border:        1.5px solid var(--panel-border);
  border-radius: 99px;
  background:    transparent;
  font-family:   var(--font);
  font-size:     0.78rem;
  font-weight:   600;
  cursor:        pointer;
  color:         var(--text-muted);
  transition:    background 0.15s, border-color 0.15s, color 0.15s;
}
.audio-btn:hover { background: var(--key-hover); border-color: var(--brand); color: var(--text); }

#play-again-btn {
  width:         100%;
  padding:       12px;
  background:    var(--brand);
  color:         var(--brand-text);
  border:        none;
  border-radius: 10px;
  font-family:   var(--font);
  font-size:     0.97rem;
  font-weight:   700;
  cursor:        pointer;
  transition:    background 0.15s;
  margin-top:    4px;
}
#play-again-btn:hover { background: var(--brand-dk); }

/* ── Responsive ──────────────────────────────────────── */
@media (max-width: 380px) {
  :root { --key-min-w: 27px; --key-h: 46px; }
  .key { font-size: 0.78rem; }
}

@media (orientation: landscape) and (max-height: 540px) {
  main { flex-direction: row; gap: 14px; justify-content: center; }
  :root { --tile-size: 38px; --key-h: 40px; --key-min-w: 24px; --gap-key: 6px; }
}
/* ── Motion (Fun/Calm) ───────────────────────────────── */
@keyframes key-pop {
  0%   { transform: translateY(0) scale(1); }
  55%  { transform: translateY(calc(-2px * var(--motion-mult))) scale(calc(1 + (0.06 * var(--motion-mult)))); }
  100% { transform: translateY(0) scale(1); }
}
@keyframes key-wiggle {
  0% { transform: rotate(0deg); }
  25% { transform: rotate(calc(-2deg * var(--motion-mult))); }
  50% { transform: rotate(calc(2deg * var(--motion-mult))); }
  100% { transform: rotate(0deg); }
}
.key.wq-press {
  animation: key-pop calc(140ms + (60ms * (1 - var(--motion-mult)))) ease-out;
}
html[data-motion="fun"] .key.wq-press {
  animation: key-pop 160ms ease-out, key-wiggle 140ms ease-out;
}

/* Tile reveal emphasis */
@keyframes tile-settle {
  0% { transform: translateY(0) scale(1); }
  60% { transform: translateY(calc(2px * var(--motion-mult))) scale(calc(1 + (0.03 * var(--motion-mult)))); }
  100% { transform: translateY(0) scale(1); }
}
.tile.wq-reveal { animation: tile-settle 220ms ease-out; }

/* Celebration layer */
.celebrate-layer{
  position: fixed;
  inset: 0;
  pointer-events: none;
  z-index: 999;
}
.celebrate-star{
  position: absolute;
  font-size: 34px;
  filter: drop-shadow(0 10px 18px rgba(0,0,0,0.35));
  opacity: 0;
  transform: translate(-50%,-50%) scale(0.4);
  animation: star-pop 900ms ease-out forwards;
}
@keyframes star-pop{
  0%{ opacity:0; transform: translate(-50%,-50%) scale(0.4) rotate(0deg); }
  35%{ opacity:1; transform: translate(-50%,-50%) scale(1.1) rotate(8deg); }
  100%{ opacity:0; transform: translate(-50%,-50%) scale(1.3) rotate(-8deg); }
}
